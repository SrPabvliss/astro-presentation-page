---
import { Hero } from '@/partials/Hero';
// import { RecentPosts } from '@/partials/RecentPosts';
import { Technologies } from '@/partials/Technologies';
import Base from '@/templates/Base.astro';
import { AppConfig } from '@/utils/AppConfig';

// import { sortByDate } from '@/utils/Posts';
import { FeaturedProjectsSection } from '../partials/FeaturedProjectsSection';
import { OtherProjectSection } from '../partials/OtherProjectSection';

// const allPosts = await Astro.glob<IFrontmatter>('./posts/*.md');
// const sortedPosts = sortByDate(allPosts);
// const lastThreePosts = sortedPosts.slice(0, 3);

const { title } = AppConfig;
const { description } = AppConfig;
---

<Base head={{ title, description }}>
  <Hero />
  <Technologies />
  <FeaturedProjectsSection />
  <OtherProjectSection />
  <!-- <RecentPosts postList={lastThreePosts} /> -->
  <!-- <CTA /> -->
</Base>
